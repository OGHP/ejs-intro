
<ul>
    <% for(var person of people) { %>
        <% if(person.name === 'heather') { %>
    <li>This is definitely <%= person.name %></li>
        <% } else { %>
    <li>This is  <%= person.name %> <% person.name %></li>
        <% } %>
    <% } %>
</ul>
<h3>This is index.ejs - the child view file</h3>
<p>This is the "home page" ane the header/footer are being populated by a wrapper that is in the layout file!!</p>
